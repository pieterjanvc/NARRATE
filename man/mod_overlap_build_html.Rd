% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_mod_overlap.R
\name{mod_overlap_build_html}
\alias{mod_overlap_build_html}
\title{Internal function to splice overlap segments into source HTML}
\usage{
mod_overlap_build_html(txt, segments)
}
\arguments{
\item{txt}{Character string, source text in HTML format}

\item{segments}{List of segments as returned by
\code{\link[=mod_overlap_compute_segments]{mod_overlap_compute_segments()}}}
}
\value{
A \code{shiny::HTML()} string
}
\description{
Tokenizes \code{txt} into alternating tag / plain-text runs (same approach as
\code{mod_highlight_server()}'s internal \code{buildHighlightedHtml()}), then for
each plain-text token clips the (already disjoint, \code{start}-sorted)
\code{segments} to that token's local plain-text-offset range and splices in a
\verb{<span>} carrying the segment's background/text colors and hover-table
HTML. A segment that straddles an HTML tag naturally splits into multiple
identically-styled spans, one per token fragment it touches - harmless,
since each fragment is independently hoverable with identical content.
}
