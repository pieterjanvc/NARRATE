% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{review_scores}
\alias{review_scores}
\title{Compute normalized coverage/specificity/utility scores for review assignments}
\usage{
review_scores(
  conn,
  review_ids,
  coverage_weight = 0.35,
  specificity_weight = 0.45,
  utility_weight = 0.2,
  error_on_incomplete = TRUE
)
}
\arguments{
\item{conn}{NARRATE database connection}

\item{review_ids}{Integer vector of \code{review_assignment.id} values to score}

\item{coverage_weight, specificity_weight, utility_weight}{Relative weights for
coverage, average specificity, and utility. Normalized internally so they
sum to 1, regardless of the scale they're passed in on. Default 0.35 / 0.45 / 0.20}

\item{error_on_incomplete}{If \code{TRUE} (default), \code{stop()} when any \code{review_id}
is missing or not in a completed status. If \code{FALSE}, such ids are silently
dropped before scoring}
}
\value{
Data frame with one row per surviving \code{review_id}: \code{review_id},
\code{reviewer_id}, \code{coverage}, \code{avg_specificity}, \code{utility}. The three metric
columns are already weight-scaled, rubric-normalized fractions (0-1) such
that \code{coverage + avg_specificity + utility} equals the total score
fraction for that review
}
\description{
Reproduces the quality score logic used in \code{inst/ai_review_app.R} as a
reusable, exported function. For each review, the raw metrics (n competencies
scored, average specificity, utility) are normalized against the range
available on that review's rubric, then scaled by the (normalized) weights.
Sentiment is intentionally excluded from this score.
}
