% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_mod_overlap.R
\name{mod_overlap_compute_segments}
\alias{mod_overlap_compute_segments}
\title{Internal function to compute merged overlap segments}
\usage{
mod_overlap_compute_segments(df, totalUsers, group_names, user_names)
}
\arguments{
\item{df}{Data frame with columns \code{user_id}, \code{group_id}, \code{start}, \code{end}
(integer, half-open, \code{start < end}, no \code{NA}s), plus any extra columns
(e.g. \code{score}, \code{note}) - anything beyond \code{id}, \code{user_id}, \code{group_id},
\code{start}, \code{end} is carried through into each segment's hover table as an
additional column (see \code{\link[=mod_overlap_hover_table_html]{mod_overlap_hover_table_html()}})}

\item{totalUsers}{Total distinct users in the unfiltered highlights input}

\item{group_names}{(Optional) Named vector, \code{group_id} -> display name}

\item{user_names}{(Optional) Named vector, \code{user_id} -> display name}
}
\value{
A list of segments, each
\code{list(start, end, bgColor, textColor, hoverHtml)} (\code{textColor} is
\code{NA_character_} when there's no cross-group conflict), sorted by \code{start}
and mutually non-overlapping. Empty list if \code{df} has no rows or covers no
gaps
}
\description{
Sweep-line over a (already filtered, malformed-row-free) highlights data
frame: computes the breakpoints formed by every distinct \code{start}/\code{end}
value, then for each atomic gap between consecutive breakpoints
determines the covering (\code{user_id}, \code{group_id}) pairs (deduped, so a user
covering via multiple raw rows counts once), the resulting background
color (always \code{\link[=mod_overlap_interpolate_color]{mod_overlap_interpolate_color()}} on the user-count scale -
a \code{group_id} conflict does not affect it), a text color override (
\verb{#ff0000} if the covering pairs span more than one \code{group_id}, \code{NA}
otherwise, meaning "leave the default text color alone"), and the
hover-table HTML.
}
\details{
Because \code{start}/\code{end} are half-open and \code{lo}/\code{hi} come from consecutive
breakpoints, a row either fully contains \verb{[lo, hi)} or is disjoint from
it - no partial-overlap case exists at this granularity, so a plain
containment check (\code{start <= lo & end >= hi}) is sufficient.
}
