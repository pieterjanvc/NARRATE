% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_mod_overlap.R
\name{mod_overlap_ui_text}
\alias{mod_overlap_ui_text}
\title{Module UI for the overlap text display}
\usage{
mod_overlap_ui_text(id)
}
\arguments{
\item{id}{Module ID}
}
\value{
Shiny UI element
}
\description{
Contains only a \code{\link[shiny:htmlOutput]{shiny::uiOutput()}} that the paired \code{\link[=mod_overlap_server]{mod_overlap_server()}}
renders the source text into, with highlight \verb{<span>}s injected around
every region covered by at least one (post-filter) highlight. Overlapping
coverage from multiple distinct users is shown as a single merged span
per atomic sub-interval, its background shaded on a scale from pale
yellow (one covering user) to green (all distinct users in the
\emph{unfiltered} highlights); if the covering highlights span more than one
\code{group_id}, the span's text (not its background) renders in red instead
of the default text color, layering the conflict signal on top of the
user-count scale rather than replacing it.
}
\details{
Hovering a span reveals a small table (user, group) of everyone covering
that segment. Showing/hiding it is pure CSS (a hidden table baked into
the span, revealed via \verb{:hover}) and never round-trips through Shiny; a
small scoped script only \emph{positions} the table (\code{position: fixed},
tracking the cursor via \code{mousemove}) so it isn't clipped by a scrollable
ancestor - e.g. callers that wrap this UI in an \code{overflow-y: auto}
container, as \code{mod_highlight_ui_text()} typically is.

Must be paired with a \code{\link[=mod_overlap_ui_filters]{mod_overlap_ui_filters()}} call using the \emph{same}
module \code{id}, and both wired up to a single \code{\link[=mod_overlap_server]{mod_overlap_server()}} call
(also using that \code{id}).
}
