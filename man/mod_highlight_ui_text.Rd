% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_mod_highlight.R
\name{mod_highlight_ui_text}
\alias{mod_highlight_ui_text}
\title{Module UI for the highlighted text display}
\usage{
mod_highlight_ui_text(id)
}
\arguments{
\item{id}{Module ID}
}
\value{
Shiny UI element
}
\description{
Contains only a \code{\link[shiny:htmlOutput]{shiny::uiOutput()}} that the paired \code{\link[=mod_highlight_server]{mod_highlight_server()}}
renders the source text into (with highlight \verb{<span>}s injected around
the currently saved selections). Also carries the selection-capture script,
since it needs to reference this element's own container id directly.
}
\details{
Must be paired with a \code{\link[=mod_highlight_ui_controls]{mod_highlight_ui_controls()}} call using the \emph{same}
module \code{id}, and both wired up to a single \code{\link[=mod_highlight_server]{mod_highlight_server()}} call
(also using that \code{id}).
}
