% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_mod_overlap.R
\name{mod_overlap_server}
\alias{mod_overlap_server}
\title{Module server for the overlap summary display}
\usage{
mod_overlap_server(id, highlights, group_names, user_names, text)
}
\arguments{
\item{id}{Module ID}

\item{highlights}{A (reactive) data frame with columns \code{id}, \code{user_id},
\code{group_id}, \code{start}, \code{end}. This is the same shape \code{\link[=mod_highlight_server]{mod_highlight_server()}}
returns, plus a \code{user_id} column the caller must add after combining
multiple users' highlight-module outputs (the highlight module's own
return value has no \code{user_id}, since a single instance of it only ever
tracks one user's session). \code{start}/\code{end} are plain-text (tag-stripped),
zero-indexed, half-open offsets into \code{text}, matching the highlight
module's convention. Rows with \code{start >= end} or \code{NA} \code{start}/\code{end} are
dropped before rendering and before computing the color scale's
denominator (treated as malformed, not as zero-length-valid highlights).
\code{id} is not relied upon internally (only \verb{(user_id, group_id)} pairs are
deduped) so it need not be unique across users. Any columns beyond \code{id},
\code{user_id}, \code{group_id}, \code{start}, \code{end} (e.g. \code{score}, \code{note}) are treated
as extra data and automatically added as additional columns in each
span's hover table, with a header auto-derived from the column name (see
\code{\link[=mod_overlap_hover_table_html]{mod_overlap_hover_table_html()}}) - no further wiring needed to surface a
new column.}

\item{group_names}{(Optional) A named vector, \code{group_id} -> display name,
used both for the group filter select's labels and the \code{group_name}
column in each span's hover table. \code{group_id} values with no entry here
(or if the argument is omitted entirely) fall back to the raw \code{group_id}}

\item{user_names}{(Optional) Same idea as \code{group_names}, for \code{user_id} ->
display name (\code{user_name} column / user filter labels)}

\item{text}{A (reactive) character string with the source text to render,
in HTML format - same convention as \code{\link[=mod_highlight_server]{mod_highlight_server()}}'s \code{text}
argument. \code{NULL}/\code{""} renders as empty output}
}
\value{
No return value; the module renders directly into its own
\code{\link[shiny:htmlOutput]{shiny::uiOutput()}} (this is a read-only summary view, not an input module)
}
\description{
Module server for the overlap summary display
}
