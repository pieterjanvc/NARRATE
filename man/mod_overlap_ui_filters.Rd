% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_mod_overlap.R
\name{mod_overlap_ui_filters}
\alias{mod_overlap_ui_filters}
\title{Module UI for overlap filter controls}
\usage{
mod_overlap_ui_filters(id, group_label = "Group", user_label = "User")
}
\arguments{
\item{id}{Module ID}

\item{group_label}{(Default = "Group"). Label for the group filter select}

\item{user_label}{(Default = "User"). Label for the user filter select}
}
\value{
Shiny UI element
}
\description{
UI has (top to bottom): a "group" multi-select (filters by \code{group_id}),
a "user" multi-select (filters by \code{user_id}), and a reset button that
restores both selects to "all selected". Choices for both selects are
populated/refreshed server-side from the distinct \code{group_id}/\code{user_id}
values present in the (unfiltered) \code{highlights} passed to the paired
\code{\link[=mod_overlap_server]{mod_overlap_server()}} call, so this UI function only lays out empty
selects - it does not take the data as an argument.
}
\details{
Must be paired with a \code{\link[=mod_overlap_ui_text]{mod_overlap_ui_text()}} call using the \emph{same}
module \code{id}, and both wired up to a single \code{\link[=mod_overlap_server]{mod_overlap_server()}} call
(also using that \code{id}).
}
